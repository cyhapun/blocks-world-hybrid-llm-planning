You are solving a Blocks World planning task.

Available actions:
- pick-up(x)
- put-down(x)
- stack(x, y)
- unstack(x, y)

Return only the action list, one action per line.
Do not include explanations, markdown, comments, or extra text.

Task:
{natural_language}